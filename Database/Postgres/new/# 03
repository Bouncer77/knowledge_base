# 03

Многоверсионность

Postgres позволяет увидеть разнае версии одной и той же строки

Номер транзакции, которая создала версию строки и номер транзакции, которая уничтожила эту строку

Снимок данных содержит только актуальную версию строк 

